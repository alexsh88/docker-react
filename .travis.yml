language: generic
sudo: required
services:
 - docker


before_install:
  - docker build -t alexsh88/docker-react -f Dockerfile.dev .

script:
 - docker run -e CI=true alexsh88/docker-react npm run test